# Session TODO List

Source transcript: `kindpath-analyser/docs/chat_transcripts/session-93f25638-3b90-436c-b50b-97f03d36c6f4.md`

## Priority 0: Stabilize Conversation Reliability

1. Fix Copilot chat readiness errors in this workspace.
- Why: The session repeatedly failed at Turns 16-21 (`Chat took too long to get ready`), blocking workflow continuity.
- Deliverables:
	- Repro steps documented.
	- Environment check script for extension/auth readiness.
	- Fallback transcript/log capture path if chat fails.
- Acceptance:
	- 5 consecutive chat turns complete without readiness timeout.

## Priority 1: Create KindPress as a First-Class Module

2. Formalize `KindPress` scope and API contract.
- Why: Session explicitly asks for a standalone memory/compression module.
- Deliverables:
	- `docs/KINDPRESS_SPEC.md` with purpose, non-goals, and architecture.
	- Public API for `fingerprint -> compressed payload -> reconstruction`.
	- Clear boundary between compression, reasoning, and validation subsystems.
- Acceptance:
	- Spec approved and mapped to existing `kindpress/press.py`, `kindpress/reason.py`, `kindpress/validate.py`.

3. Add transport-oriented compression profile (optional mode).
- Why: User asks whether this can reduce data transfer/upload times.
- Deliverables:
	- Secondary profile for network payloads (not just memory fidelity).
	- Benchmarks for payload reduction and decode latency.
- Acceptance:
	- Measurable reduction vs baseline serialization with documented tradeoffs.

## Priority 2: Tag/Constant Governance and Continuous Retrospective Updates

4. Implement tag revision gate through the semantic-historical reasoner.
- Why: Session requires every tag change to be reasoned before acceptance.
- Deliverables:
	- `propose_tag_revision()` workflow with reasoner verdict.
	- Impact report: inferred consequences, confidence, affected record counts.
	- Block/allow policy with rationale capture.
- Acceptance:
	- No direct tag mutation path bypasses reasoner checks.

5. Build retrospective metadata requalification pipeline.
- Why: Session emphasizes continuous-improvement with retrospective updates.
- Deliverables:
	- Re-qualification job to replay updated tag logic over historical profiles.
	- Versioned metadata updates with provenance and reversible checkpoints.
- Acceptance:
	- Historical records can be recalculated under new tag versions without data loss.

## Priority 3: Fork/Selection Event Canonical Audit Trail

6. Build cross-cutting fork event log module.
- Why: Session identifies missing changelog for selection/fork moments.
- Deliverables:
	- `seedbank/fork_log.py` (or `seedbank/changelog.py`).
	- Event types:
		- tag revision events (`revise_tag`)
		- record fork events (`add_reading` lineage changes)
	- Query API: `get_fork_events(since=None, tag=None, record_id=None)`.
- Acceptance:
	- Every fork/revision event recorded with timestamp, reason/source, and affected count.

7. Expose optional org-level fork signal mirror.
- Why: Session suggests cross-repo implications (`kindpath-canon`, `kindpath-q`, `KindSense`).
- Deliverables:
	- Minimal `.github`-level aggregated feed/export.
	- Mapping from local event -> org signal schema.
- Acceptance:
	- Consumers can subscribe to doctrinally relevant k-space changes.

## Priority 4: HMOE-Centric Error-Correction + Validation Framework

8. Build compressed-vs-decompressed drift harness using HMOE.
- Why: Session asks to validate change proposals in compressed sample space, then compare decompressed drift.
- Deliverables:
	- Test harness for A/B runs on compressed and reconstructed datasets.
	- Drift metrics and HMOE thresholds.
	- Auto-stop at max tolerated HMOE boundary.
- Acceptance:
	- Deterministic report showing drift by chunk size and revision candidate.

9. Add adaptive chunk sizing to maximize efficiency within HMOE bounds.
- Why: Session proposes scaling chunks until maximum HMOE state is reached.
- Deliverables:
	- Search strategy for chunk size (coarse-to-fine).
	- Pareto output: compression ratio vs reconstruction drift.
- Acceptance:
	- Selected chunk profile is reproducible and policy-driven.

10. Add raw-data inconsistency detector via error-margin spikes.
- Why: Session notes spikes may indicate inconsistent source data.
- Deliverables:
	- Spike detector and confidence banding.
	- Differentiation between likely sensor/input anomaly vs meaningful divergence.
- Acceptance:
	- Alerts include evidence and suggested validation action.

## Priority 5: Terminology and Doctrine Alignment

11. Replace "breeding" terminology with multi-context language.
- Why: Session explicitly requests wording shift.
- Candidate replacements:
	- `confluence`
	- `lineage weaving`
	- `branch recombination`
	- `contextual convergence`
- Deliverables:
	- Terminology ADR + repository-wide glossary updates.
- Acceptance:
	- No user-facing docs rely on legacy term where new vocabulary is intended.

12. Add explicit HMoE framing to existing analyser outputs.
- Why: Session insight: capabilities already exist but framing is implicit.
- Deliverables:
	- Document mapping: Creative Residue, LSII, Groove Deviation -> HMoE constructs.
	- Explanatory section in docs and reports.
- Acceptance:
	- Users can interpret metrics through the same conceptual lens used in design discussion.

## Priority 6: Research and Reuse Scan

13. Research production analogues for error-correction architecture.
- Why: Session asks whether similar mechanism exists and can be adopted.
- Scope:
	- error-correcting codes (ECC/FEC), CDC logs, event sourcing, schema evolution, semantic drift detection, provenance systems.
- Deliverables:
	- `docs/research/error-correction-landscape.md` with adopt/adapt/reject recommendations.
- Acceptance:
	- At least one near-term adoption candidate selected for implementation.

## Suggested Implementation Sequence (PR-sized)

1. PR1: Fork event log + query API + tests.
2. PR2: Tag revision gate through reasoner + impact report.
3. PR3: HMOE drift harness (compressed/decompressed) + adaptive chunk sizing.
4. PR4: Terminology migration + docs framing updates.
5. PR5: Transport compression profile + benchmark report.
6. PR6: Org-level fork signal export + cross-repo integration notes.

## Test Coverage Required

1. Unit tests for event logging integrity and query filtering.
2. Unit/integration tests for tag revision gate pass/fail logic.
3. Deterministic tests for reconstruction drift + HMOE thresholds.
4. Regression tests ensuring retrospective requalification is reversible.
5. Performance tests for compression profile (size/latency).

## Open Questions to Resolve Before Build

1. Should `KindPress` live only in `kindpath-analyser` first, or be extracted as a shared package from day one?
2. What is the canonical HMOE threshold policy per data domain (audio, text, relational logs)?
3. Is org-level changelog event propagation push-based (webhook/file) or pull-based (scheduled index rebuild)?
4. Which terminology is final for the migration away from "breeding"?

